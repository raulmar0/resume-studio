import { GuestDashboard } from "@/components/guest/GuestDashboard";

export const metadata = { title: "Guest dashboard · Resume Studio" };

export default function GuestDashboardPage() {
  return <GuestDashboard />;
}
